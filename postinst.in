#!/bin/bash

echo "Post install depmod and udev reload rules"
dkms add -m usb_skeleton -v ${VERSION}
dkms build -m usb_skeleton -v ${VERSION}
dkms install -m usb_skeleton -v ${VERSION}
depmod
udevadm control --reload-rules